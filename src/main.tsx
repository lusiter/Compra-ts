import React from 'react'
import ReactDOM from 'react-dom/client'

import Appxx from './Appxx.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <Appxx />
  </React.StrictMode>,
)
